import './App.css'

export default function Header() {
  return (
    <header>
      <h1 className="title">Needle Drop</h1>
      <span className="tagline">my collection, one drop at a time</span>
      <p className="subtitle">A catalog of the albums I own — physical and digital — searchable and filterable by genre.</p>
    </header>
  );
}